# Pandas pipeline tasks

This directory contains tasks for working with pandas, following strict project requirements:
- Python 3.9 on Ubuntu 20.04 LTS
- pandas==2.2.2, numpy==1.25.2 available at runtime
- Only `import pandas as pd` allowed unless otherwise noted
- Files must be executable and start with `#!/usr/bin/env python3`
- Code must follow pycodestyle (2.11.1)
- All modules, classes, and functions must have documentation

Task 0: Implement `from_numpy(array)` to create a DataFrame from a NumPy array with columns labeled A–Z (capitalized), in order.
